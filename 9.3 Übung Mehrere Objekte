Erzeugen Sie nun zwei unterschiedliche Objekte und geben deren Werte (Rückgabe der new-Operation)
auf der Konsole aus.


Lösung:

public class Student {
    String name;
    int alter;
    String studiengang;
}

public class Main {
    public static void main(String[] args) {
        Student student1 = new Student();
        student1.name = "Max Mustermann";
        student1.alter = 21;
        student1.studiengang = "Informatik";
        
        Student student2 = new Student();
        student2.name = "Anna Schmidt";
        student2.alter = 20;
        student2.studiengang = "Physik";
        
        System.out.println("Name: " + student1.name + ", Alter: " + student1.alter + ", Studiengang: " + student1.studiengang);
        System.out.println("Name: " + student2.name + ", Alter: " + student2.alter + ", Studiengang: " + student2.studiengang);
    }
}


Ausgabe:

Name: Max Mustermann, Alter: 21, Studiengang: Informatik
Name: Anna Schmidt, Alter: 20, Studiengang: Physik
