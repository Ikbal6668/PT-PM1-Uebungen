Erzeugen Sie ein Array der Länge 10 und befüllen Sie es mit den Quadratzahlen: Feld i: i*i



Code:


  public static void main(String[] args) {

    int[] quadratzahlen = new int[10];
        for (int i = 0; i < quadratzahlen.length; i++) {
            quadratzahlen[i] = i * i;
   }
}

Das Array `array` wird mit der Länge 10 erstellt. In der Schleife wird jedes Element
des Arrays mit dem Quadrat des Index-Werts initialisiert. Das heißt, das Element an 
der Position `i` im Array wird den Wert `i * i` erhalten. Nach Abschluss der Schleife
enthält das Array die Quadratzahlen von 0 bis 9 an den entsprechenden Positionen.
